// Define user roles
export enum UserRole {
    ADMIN = '2',
    JOINER = '1',
    GUEST = '0'
}

// Define region type
export enum Region {
    BKK = 'BKK',
    EAST = 'EAST',
    CEN = 'CEN',
    PARI = 'PARI'
}
